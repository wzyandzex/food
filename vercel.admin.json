{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm install --frozen-lockfile && pnpm -F @kaifan/admin build",
  "outputDirectory": "apps/admin/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "github": {
    "silent": true
  }
}
